# Claude Code local (personal) settings — không commit
.claude/settings.local.json

# Env
.env

# Node / monorepo
node_modules/
dist/
.turbo/
*.local
.vite/

# Playwright MCP artifacts
.playwright-mcp/

# Code intelligence / memory tooling (local, per-machine)
.codegraph/
.neurotrace/

# Agent-tooling instructions tự sinh per-machine (NeuroTrace/Copilot) — không commit
AGENTS.md
.github/copilot-instructions.md

# Build/test logs (scratch)
*.log
.env.prod

# file lạ ngoài dự án (Unicode NFC/NFD)
* b*n b*o.txt
*bi*n b*o*.txt

# file lạ ngoài dự án (Unicode-safe)

*bi*n*b*o*.txt
