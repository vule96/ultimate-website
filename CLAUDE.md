# ultimate-website

Dự án website blog cá nhân (FE + BE + AI). Xây dựng mới từ đầu.

## Vai trò (persona)

Bạn là **senior fullstack + DevOps** nhiều năm kinh nghiệm, chuyên tối ưu code, quy trình nghiệp vụ, UI/UX và hạ tầng server. Ra quyết định ở mức đó: chọn giải pháp đúng-và-gọn thay vì cồng kềnh, nêu trade-off rõ ràng, bảo mật + hiệu năng + khả năng vận hành luôn nằm trong cân nhắc, không vàng thau lẫn lộn.

## Bối cảnh & thiết kế

Tài liệu phân tích kiến trúc, stack, chi phí và roadmap đầy đủ nằm ở:
**`docs/personal-blog-ai-analysis.md`** (đọc file này trước khi bắt đầu bất kỳ việc gì).
Bản xem đẹp: **`docs/personal-blog-ai-analysis.html`** (standalone, tự mở trong trình duyệt được — UTF-8, sáng/tối, nền kem dịu mắt; **không dùng Artifact**).

**Kiến trúc vận hành thực tế** (sơ đồ, luồng BE↔FE, cache từng tầng, AI tương lai): **`docs/architecture.md`**.

**Trạng thái & roadmap trực quan** (timeline slice, stack, việc tồn đọng): **`docs/status-roadmap.html`** (standalone, mở trực tiếp trong trình duyệt).

Tóm tắt định hướng đã chốt:
- Mục tiêu: cân bằng học + sản phẩm dùng thật. Ngân sách ~15–40 USD/tháng.
- Kiến trúc: **KHÔNG microservice thật** → Modular Monolith (Go) + 1 AI worker (Python/LangChain) tách riêng.
- FE công khai (blog): **Next.js (App Router)** + Tailwind + shadcn/ui (SSR/SSG cho SEO).
- FE admin (dashboard): **React SPA — Vite + React + TanStack Router (file-based)** (KHÔNG dùng Next.js). Auth: **Google OAuth qua Go core theo BFF pattern** (Go đổi code → session cookie httpOnly) + allowlist email. Dùng chung `packages/ui` + `packages/types` với app web.
- Editor: Tiptap (hoặc Novel) — nằm trong app admin.
- Storage ảnh: Cloudflare R2 (presigned URL). DB: Postgres + pgvector.
- Monorepo: pnpm workspaces + Turborepo.
- AI: chatbot RAG, research + tự viết bài duyệt qua Telegram (human-in-the-loop), trợ lý viết.
- Roadmap 6 phase (0 → 5); bắt đầu từ Phase 0 (nền móng) + Phase 1 (blog cơ bản).

## Trạng thái hiện tại

Triển khai Phase 1 theo **4 slice tuần tự** (spec từng slice ở `docs/superpowers/specs/`).

> **🩺 Issue tracker (2026-07-11):** Senior code review toàn codebase (Go core B+, admin B+, web B) — findings đầy đủ ở **`docs/reviews/2026-07-11-senior-code-review.md`**. Đây là **nơi note & track mọi issue**: khi resolve issue nào phải đánh dấu `✅ RESOLVED (ngày, commit)` ngay tại finding đó trong file; issue mới phát hiện cũng note vào đây. **Slice 5a DONE: C1, H1, H4, A1, A4, M7. Slice 5b DONE: W1, W2, H2, H3, A2, A3 (+M6, L7). Slice 5c DONE: W3, W4, W5, W6, W7 + low SEO + next/font + A5–A9 + admin low. Slice 5d DONE: M1, M2, M3, M4, M5 + outbox (chuẩn bị Phase 2). Slice 5e DONE: L8, L9, L10, L11, L12.** Toàn bộ finding Critical/High/Medium/Low đã **yêu cầu** resolve đều DONE; backlog Low còn mở chỉ còn **L1–L6** (chưa được yêu cầu ưu tiên, note sẵn trong tracker chờ khi cần).
>
> **📍 Điểm hiện tại (2026-07-18):** **Phase 1 HOÀN TẤT** (Slice 1→4) + **Slice 5a–5e, 6, 7, 8, 9, 10, 11, 12, 13 DONE** + **Slice 14 DONE (CI/CD)** + **Slice 15 DONE (Next 16 + React 19 + next-intl v4 — full FE modernization)**. **Bước kế tiếp đã chốt: (1) Slice 16 — Production edge (Caddy auto-TLS + domain + DNS Cloudflare + firewall chỉ mở 80/443 + backup `pg_dump→R2` cron) — BLOCKER vì compose hiện publish port trần, chưa có TLS; (2) Slice 17 — CD live: dựng VPS → điền secrets `VPS_*` + vars public URL → bỏ comment auto-trigger `deploy.yml` → verify `/healthz`; (3) Phase 2 — AI (RAG chatbot pgvector + Claude API, Telegram auto-write human-in-loop, trợ lý viết).** **VPS reco (0.1% expert):** footprint idle nhẹ (app ~130MB + observability ~170MB, đo thật), RAM là ràng buộc chính (build ở CI/GHCR, VPS chỉ pull+run) → **Hetzner CPX31 (4 vCPU/8GB/160GB ~$15/mo)** sweet spot (dư cho AI + observability); CX22 4GB ~$4.5 viable nếu bỏ observability + swap; đặt Cloudflare proxy trước origin (cache SSG edge → latency origin ít quan trọng, chọn theo giá); DB self-host + backup. Phân tích đầy đủ: `docs/status-roadmap.html` + `docs/cicd.html`. **Slice 15:** web lên **Next 14→16.2.10 + React 18→19.2 + next-intl v3→v4**, admin + packages/ui lên **React 19**; vá 5 CVE Next 14 → **gỡ `.trivyignore.yaml`** (Trivy xanh không ignore). Chi tiết: async request APIs (codemod `await params` 7 file), next-intl v4 (`NextIntlClientProvider locale` prop — codebase đã dùng API v4-style), **Turbopack build mặc định chạy tốt** (không cần webpack fallback, 55 trang SSG + ISR giữ), **ESLint 9 flat config** (`eslint-config-next` 16 native flat, script `next lint`→`eslint .`, đưa lint web lại vào CI), **pnpm overrides ép single react/react-dom/@types 19 toàn workspace** (dedupe — radix qua ui gây "Invalid hook call/useMemo null"), `pnpm-workspace.yaml allowBuilds` (sharp/swc/esbuild/unrs-resolver). Fix nhỏ: React 19 bỏ global `JSX` namespace (admin ImageNode `import type JSX`), rule mới `react-hooks/set-state-in-effect` (use-theme disable có justification), vitest inline next-intl (resolve `next/navigation`), test mocks thêm `blurhash` + cast Metadata OG/Twitter union. **Verify: full workspace 8/8 turbo task + tsc 0 lỗi + web 101/admin 39/ui 3 test + Docker FE build + CI PR #4 XANH (core/web/admin/security) + E2E LIVE DONE (stack prod thật + Google OAuth).** E2E lộ 2 bug admin (đã fix, không phải hồi quy Next/React thuần mà React 19/cache gotcha): (1) **radix Select không cập nhật label khi `value` đổi sau mount (React 19)** → form sửa bài trạng thái trống → zod chặn submit im lặng → fix `key={field.value}` remount Select + regression test; (2) **form hiện giá trị cũ khi vô lại** — loader `ensureQueryData` trả cache detail cũ (invalidate không ép refetch data đã có) → fix `onSuccess` seed `qc.setQueryData(detail, post)`. **Lưu ý OAuth prod:** `.env.prod` phải điền `GOOGLE_CLIENT_ID/SECRET` + `ADMIN_ALLOWLIST` (BFF redirect_uri về core `:8080/auth/{google,reader}/google/callback` — đăng ký Console + Test users). Nợ còn: `NEXT_PUBLIC_API_URL` build-arg web (nợ Slice 14). Spec: `docs/superpowers/specs/2026-07-18-slice15-next16-react19-design.md`. **(2026-07-19) ESLint web dọn sạch (0 warning, `<a>`→Link). Fix bug homepage/tags rỗng lần đầu vào: `/` + `/tags` fetch-all lúc build (BUILD_WITHOUT_API → buildSafe []) bị bake SSG rỗng → ISR phục vụ stale rỗng → đổi `export const dynamic="force-dynamic"` (luôn SSR fresh, SEO giữ). Verify live: home `/` ra 12 bài không cần bust cache. Doc CI/CD: `docs/cicd.html`.** **UI REDESIGN đợt 1 — DONE (2026-07-19, branch `slice16-mach-ui-redesign`):** bỏ 3 dấu hiệu "AI-design" đắt nhất, KHÔNG đụng kiến trúc. (1) **Brand accent xanh #1668e3 → teal sâu #0f6e63** (dark #3aa99b) tại 1 nguồn token (`globals.css --accent` + `categories.ts ACCENT`) → logo/CTA/link/ranking/chip fallback tự đổi; teal trên nền kem thoát cả 3 cluster AI (không cream+terracotta, không blue, không acid-on-black). (2) **Category giữ 10 nhưng hạ bão hoà** về bản muối/trầm (giữ hue = giữ nhận diện); IT lệch khỏi blue chói. (3) **FeaturedLead** mới (1 lead lớn + 2 secondary) tạo hierarchy; `ArticleList skipCount=3` bỏ trùng, results count = tổng, bỏ highlighter vàng "Mới nhất". Verify: 105 test web xanh (+4 featured-lead), lint+tsc sạch, Playwright live home/detail × light/dark/mobile. **⚠️ :3000 là Docker prod image cũ — rebuild `docker compose -f docker-compose.prod.yml build web && up -d web` để thấy UI mới (verify chạy trên dev :3100).** **UI REDESIGN đợt 2 — DONE (2026-07-19):** (finding 4,5,7) **serif đọc Source Serif 4** (next/font, vietnamese subset) cho `.article-body` + deck/excerpt (lead + list row) → editorial thật; display Roboto giữ heading, mono cho data (fix `tailwind font-serif` token Lora hardcode → `var(--font-serif)`); **bỏ icon lucide CategoryRail** → nav typography-only, active = teal tint + thanh màu category bên trái; **typographic cover** (bìa chữ) cho bài không ảnh — FeaturedLead lead + secondary hết khối màu trơ. Verify: 105 test web xanh, tsc+lint sạch, Playwright live light/dark (body serif dấu tiếng Việt chuẩn, rail hết icon, lead 'IT' bìa chữ). **Đòn còn (nhỏ, chưa cần): mono discipline label chi tiết (finding 6 — display face gánh label thay mono), dọn `--highlight` token mồ côi, ưu tiên lead = bài CÓ cover, category light/dark hex riêng cho WCAG chặt.** Spec: `docs/superpowers/specs/2026-07-19-slice16-mach-ui-redesign-design.md` · Brief gốc: `docs/superpowers/specs/2026-07-19-mach-ui-redesign-brief.md`. **⚠️ BẢN NÀY ĐÃ BỊ THAY BỞI NEWSROOM REDESIGN bên dưới.** **NEWSROOM REDESIGN — DONE (2026-07-19, branch `newsroom-redesign`):** feedback "vẫn ra AI, chưa tự nhiên" → brainstorm lại từ đầu qua `frontend-design` + 3 mockup artifact (A teal product / B đỏ wire / **C chốt**), user chốt bản C + brand **cam-đỏ vermilion**. THAY hẳn UI tạp chí Mạch (Slice 6–16) sang **digital newsroom**: (a) **token trắng lạnh** `#fbfbfc` (bỏ kem) + brand vermilion `#e1442b`/dark `#ff6a4d` + 6 màu section (tech/fin/life/cul/dev/book); **1 họ font Be Vietnam Pro** toàn site (bỏ Roboto/Space Mono/Source Serif, bỏ mono chrome "SỐ 128"); (b) **chrome**: Ticker headline chạy ("Trực tiếp" + ping, `listLatest(8)` trong layout, reduced-motion tĩnh) + Masthead top-rule 3px + SectionNav gạch chân (thay SubNav); (c) **homepage theo CHUYÊN MỤC** (`sections.ts` nhóm 11 category → 4 section) — `NewsroomBoard` Hero (lead title-đè-ảnh + 4 mini) + SectionBand màu section + Rail (Top xem nhiều vermilion + chips + bản tin); search → grid kết quả; (d) detail/tags theo chrome mới (kicker màu section, meta sans tabular). **Xóa toàn bộ component magazine cũ mồ côi.** Verify: tsc + **99 test** web + lint xanh; Playwright home/detail/tags × light/dark/mobile (dev :3100); **Docker prod build OK + verify `:3000`**. Spec: `docs/superpowers/specs/2026-07-19-newsroom-redesign-design.md`. Mockup: artifact `mach-newsroom-c.html`. Nợ nhỏ: finance band ẩn khi các bài finance lọt hết vào hero top-5; TagBadge/PostCard dùng hex category (chưa theme-aware var); footer/newsletter dùng localNewsletterService (chưa flip API — cần khớp Slice 13). **Branch chưa merge/push.** **NEWSROOM POLISH — DONE + merged main (2026-07-19):** (1) finance band hết mất — lead ưu tiên bài CÓ cover, band chỉ loại đúng lead (không loại cả hero top-5); (2) newsletter flip `apiNewsletterService` (`POST /subscribers`, verify 201 live); (3) `sectionColorForTag` theme-aware cho TagBadge/PostCard (dark contrast). Verify Playwright (4 band gồm finance + lead ảnh, tags dark chip section). **Build prod chậm 15ph do WSL2 RAM mặc định thấp → ghi `%UserProfile%\.wslconfig` (memory=12GB/processors=8/swap=4GB) → build lại ~1-2ph.** **SLICE 16 EDGE (local-prep) — DONE (branch `slice16-prod-edge`, chưa merge):** Caddy reverse-proxy auto-TLS + backup, phần làm/test được KHÔNG cần VPS. `Dockerfile.caddy` (Caddy 2.10 + plugin caddy-dns/cloudflare — 2.8.4 lỗi `zapslog.HandlerOptions`), `Caddyfile` env-driven 3 site (web/core/admin, `tls {internal|email}`, header HSTS/nosniff/Referrer + gzip, DNS-01 acme_dns comment sẵn), `docker-compose.edge.yml` (caddy 80/443 + `!reset` publish port app), `scripts/backup-db.sh` (pg_dump compose-exec `--env-file` | gzip → R2 aws-cli-container bucket riêng + prune; **parse .env.prod AN TOÀN không `source`** vì value có `<`), `.gitattributes` ép LF `*.sh`/Caddyfile. Verify local: compose merge (chỉ caddy publish), build Caddy OK, route web/core/admin qua TLS internal (http→https 308 + 3×200 + header), pg_dump thật 8KB + `aws s3 cp/ls/rm` qua MinIO + prune logic. **Nợ bước VPS (Slice 17): firewall ufw 80/443/22, `NEXT_PUBLIC_API_URL` build-arg web, OAuth redirect prod https. `.env.prod` hiện STORAGE_ENDPOINT còn placeholder `<account>` (R2 chưa cấu hình thật).** Spec: `docs/superpowers/specs/2026-07-19-slice16-prod-edge-design.md`. Slice 14: 3 workflow `.github/workflows/` — `ci.yml` (PR+push: `dorny/paths-filter` → job core Go 1.25.1 + Postgres 16 service container tạo `blog_test` + gofmt/vet/build/test, job web + admin turbo lint/test/build filtered, đồng bộ admin `tsc --noEmit` qua `build`), `release.yml` (push `main`: matrix build+push 3 image `ghcr.io/vule96/ultimate-website/{core,web,admin}:latest+sha-<short>` via buildx + GHA cache, build-arg public URL từ `vars.NEXT_PUBLIC_MEDIA_HOST`/`vars.ADMIN_API_URL`), `deploy.yml` (**DORMANT** `workflow_dispatch`: SSH `appleboy/ssh-action` → one-shot Atlas migrate → `docker compose -f docker-compose.prod.yml -f docker-compose.deploy.yml pull+up` → healthcheck `/healthz`) + `docker-compose.deploy.yml` (override `build: !reset null` → `image: ghcr.io/...:${TAG:-latest}`). **Verify: CI XANH TOÀN BỘ trên PR #3 (core/web/admin/security) + actionlint sạch + `docker compose config` merge đúng. Branch protection `main` đã bật: require PR + status checks core/web/admin/security.** CI thực chạy phát lộ + đã fix một loạt vấn đề tiềm ẩn: (1) **govulncheck + Trivy tìm vuln THẬT** → upgrade Go deps (pgx→5.9.2, grpc→1.79.3, go-jose→4.1.4, otel→1.43, x/crypto→0.52, x/net→0.55, quic-go→0.59.1) + **Go toolchain 1.25.1→1.25.12** (vá stdlib crypto/tls,x509,net/http... GO-2026-*); govulncheck local "No vulnerabilities found"; (2) **Dockerfile non-root** (DS-0002): core `USER nonroot`, admin chuyển `nginxinc/nginx-unprivileged` (uid 101, listen 8080, compose `5173:8080`); (3) **Next.js 14 — 5 CVE HIGH baseline `.trivyignore.yaml` hạn 2026-09-15 → Slice 15**; (4) CI infra: node 20→22 (pnpm 11.10), `go test -p 1` (tránh race migrate chung blog_test), readers TestMain tạo citext + không re-migrate posts (fix `duplicated key` DB sạch), `turbo.json build.env` khai báo `BUILD_WITHOUT_API`/`NEXT_PUBLIC_*` (turbo strict-env strip → web build fetch API thật), `.gitattributes *.go eol=lf`, paths-filter gồm `ci.yml`. **Backlog mới:** **Slice 15 — migration Next 14→16 + React 18→19 + next-intl v3→v4** (gỡ `.trivyignore` Next; brainstorm→spec→TDD→E2E); **ESLint web** (chưa setup — `next lint` bị bỏ khỏi CI, chỉ test+build); web thiếu `NEXT_PUBLIC_API_URL` build-arg; image FE environment-specific; CD chưa verify live (chờ VPS). Spec: `docs/superpowers/specs/2026-07-18-slice14-cicd-design.md`. **— (dưới đây là context Slice 13):** **Slice 13 DONE (Backend consumer Mạch — auth người đọc + bookmark + newsletter THẬT + rate limit + view dedupe).** Slice 13 thay toàn bộ mock localStorage bằng backend thật qua 15 task TDD (subagent-driven): module Go mới `internal/modules/readers` (reader auth **Google OAuth BFF** tái dùng `auth.OAuthProvider`, session key riêng `reader_id` tách khỏi admin, KHÔNG allowlist; bookmark Postgres; subscriber citext độc lập), `shared/ratelimit.PerIP` (Redis fixed-window per-IP, fail-open), `posts.ViewDeduper` (Redis SET+TTL 1 view/người/bài/ngày, fail-open) wired vào `POST /posts/:slug/view`; FE `apps/web` flip service seam sang API (`credentials:include`), store hydrate/optimistic-bookmark-rollback/logout, auth modal → nút "Tiếp tục với Google" (full-page BFF redirect). Migration Atlas `add_readers` (readers/bookmarks/subscribers + citext + FK cascade). **Final whole-branch review (0 Critical)** đã fix: hydrate toàn site (ReaderHydrator ở layout), CORS default +`:3000`, `TRUSTED_PROXIES` env + `SetTrustedProxies` (ClientIP không spoof XFF), logout `RequireJSON`, `SafePath` reject control-char, security fix parser-differential email (`addr.Address`). **Backend `go test ./...` xanh + FE 101 test xanh + build production OK.** **⚠️ Verify E2E live CÒN LẠI (thao tác tay của bạn):** (1) thêm reader redirect URI `http://localhost:8080/auth/reader/google/callback` (dev) + URL prod vào **Google Console**; (2) chạy stack có **Redis** (`.env`: `REDIS_URL`, `VIEW_DEDUP_SALT`, `CORS_ALLOWED_ORIGINS` gồm `:3000`, `READER_REDIRECT_URL`, `WEB_BASE_URL`); (3) test login Google thật → bookmark save/reload giữ → newsletter → rate limit 429 (spam >5/phút subscribe) → view dedupe (2 lần cùng session = +1 view). Nợ mới (backlog, chưa chặn merge): FK cascade chưa có test (AutoMigrate không tạo FK, chỉ migration có); unsubscribe flow + reader profile/GDPR delete (Phase 2). Spec: `docs/superpowers/specs/2026-07-17-slice13-backend-consumer-design.md` · Plan: `docs/superpowers/plans/2026-07-17-slice13-backend-consumer.md`. Blog verify live; storage R2 (dev=MinIO/prod=R2). Backlog senior review còn **L1–L6** (low). Roadmap trực quan: **`docs/status-roadmap.html`**.

- ✅ **Slice 1 — DONE**: nền móng Go core + module `posts` (CRUD + tags), chạy end-to-end.
  Spec: `docs/superpowers/specs/2026-07-05-slice1-posts-foundation-design.md`.
- ✅ **Slice 2 — DONE**: auth Google OAuth (BFF) — `internal/modules/auth` + session server-side (scs + Postgres), allowlist email qua env, middleware `RequireAuth` bọc POST/PUT/DELETE `/posts`. Verify end-to-end với Google thật.
  Spec: `docs/superpowers/specs/2026-07-05-slice2-auth-oauth-design.md`.
- ✅ **Slice 3a — DONE**: monorepo (pnpm + Turborepo) + `apps/admin` (Vite + React + TS + Tailwind + shadcn/ui, theme HiveQ) + auth BFF (LoginPage, AuthProvider, ProtectedRoute) + Dashboard placeholder + CORS ở core. Advanced TS: Zod (`packages/types`) + branded IDs + type-fest + ts-reset.
  Spec: `docs/superpowers/specs/2026-07-06-slice3a-admin-shell-design.md`.
- ✅ **Slice 3b — DONE**: quản lý posts qua UI (TanStack Query + react-hook-form + zodResolver) — danh sách (bảng, filter status/tag, search debounce, phân trang qua URL), form tạo/sửa, xoá (confirm); Dashboard nối API thật (stat cards + recent). Core: thêm `q` search (ILIKE) + `GET /posts/stats`. Content tạm bằng textarea → `content_html`.
  Spec: `docs/superpowers/specs/2026-07-07-slice3b-posts-crud-design.md`.
- ✅ **Slice 3c — DONE**: rich editor **Tiptap + Lexical** (interface chung `PostEditorProps`, chọn qua flag `VITE_EDITOR`, code-split; HTML là cầu nối nạp nội dung, lưu `content_json` native best-effort; baseline parity + extras table/task-list/highlight; Lexical có `ImageNode` tự viết) + module `media` (presigned PUT S3-compatible: MinIO dev / R2 prod, `POST /media/presign` bọc auth) + chart Dashboard nối `GET /posts/stats/timeseries` (zero-fill theo tháng).
  Spec: `docs/superpowers/specs/2026-07-07-slice3c-editor-media-design.md`.
- ✅ **Slice 3d — DONE**: admin routing chuyển sang **TanStack Router** (file-based `src/routes/`, type-safe search params qua `validateSearch`, route loaders + `ensureQueryData`, auth guard `beforeLoad`, `useSuspenseQuery` + `pendingComponent`/`errorComponent`). Bỏ `react-router-dom`, `AuthProvider`, `ProtectedRoute`.
  Spec: `docs/superpowers/specs/2026-07-07-slice3d-tanstack-router-design.md`.
- ✅ **Slice 3e — DONE**: bảng dữ liệu dùng **TanStack Table** qua component `DataTable<TData>` chung (headless, manual server-side sort/pagination/filter, column visibility toggle); `PostsTable` migrate sang column defs typed (`createColumnHelper`, module augmentation `TableMeta.onDelete`); core thêm `sort`/`order` (whitelist ORDER BY); sort state ở URL search.
  Spec: `docs/superpowers/specs/2026-07-08-slice3e-tanstack-table-design.md`.
- ✅ **Slice 4 — DONE**: `packages/ui` (`@ultimate/ui`) chung — shadcn + theme, `apps/admin` đã migrate sang dùng chung; `apps/web` — Next.js 14 (App Router, React 18, Tailwind v3) blog công khai: trang chủ (danh sách + phân trang), `/blog/[slug]`, `/tags`, `/tags/[slug]`; SSG + ISR (`revalidate = 60`); luôn ép `status=PUBLISHED` và `notFound()` cho bài non-PUBLISHED (kể cả biết slug DRAFT); SEO đầy đủ — metadata/OG mỗi bài, `sitemap.xml`, `/rss.xml`, `robots.txt`. 19 test web + 30 test admin xanh; build @ultimate/ui (typecheck) + admin + web xanh. **Verify E2E live DONE**: chạy core + web thật với dữ liệu seed — trang chủ/list, `/blog/[slug]`, draft → 404, `/tags/[slug]`, sitemap/rss/robots đều OK; `generateStaticParams` prerender đúng slug PUBLISHED.
  Spec: `docs/superpowers/specs/2026-07-10-slice4-web-public-blog-design.md`.
- ✅ **Slice 5a — DONE (Security & Data-loss Hardening, đợt 1)**: core visibility policy session-aware (anonymous chỉ thấy PUBLISHED ở list/detail/tags, `GetBySlug` 404 bài non-published, `/posts/stats*` sau `RequireAuth` — C1, M7); presign ký `Content-Length` enforce 5MB (H1); middleware `jsonmw.RequireJSON` chặn CSRF simple-request + config fail-fast `samesite=none⇒secure` (H4, L11 một phần); admin form hết bị background refetch ghi đè + `contentJson` dùng ref (A1, A4). Verify curl E2E + toàn bộ test core/admin xanh + security-review.
  Spec: `docs/superpowers/specs/2026-07-11-slice5a-security-hardening-design.md` · Plan: `docs/superpowers/plans/2026-07-11-slice5a-security-hardening.md`.
- ✅ **Slice 5b — DONE (Production-readiness Hardening, đợt 2)**: web path-based pagination `/page/[n]` + `/tags/[slug]/page/[n]` (component chung `PostsPage`, soft-404) khôi phục SSG+ISR thật — verify production `next build && next start` `/` vs `/page/2` khác nhau (W1, W2); core `http.Server` timeouts + graceful shutdown + pool DB + `/healthz` ping DB (H2, M6, L7); middleware `reqlog` (request ID + contextual slog, ghi lỗi 500) (H3); admin `beforeLoad`/`QueryCache.onError` chỉ redirect khi 401, giữ vị trí sau login qua `sessionStorage` + open-redirect guard (A2, A3). Test web/core/admin xanh + security-review sạch.
  Spec: `docs/superpowers/specs/2026-07-11-slice5b-prod-readiness-design.md` · Plan: `docs/superpowers/plans/2026-07-11-slice5b-prod-readiness.md`.
- ✅ **Slice 5c — DONE (Polish FE, đợt 3 nhóm A)**: web SEO đầy đủ — sitemap/rss để throw (ISR giữ bản tốt cuối) (W3); metadata tag pages + JSON-LD `BlogPosting` + OG image fallback + RSS `atom:link`/language/lastBuildDate (W4, W5, low); `not-found.tsx` + `error.tsx` branded (W6); sanitize `content_html` server-side (rehype-sanitize, RSC) + CSP header không-nonce (giữ SSG) + `nosniff`/`Referrer-Policy` + gate media host (W7); migrate `next/font` (Lora+Inter) + chuẩn hoá weight. Admin TS tightening: tagKeys ownership + invalidate tags (A5), `TableMeta.onDelete?` (A6), `PostStatus` union end-to-end (A7), AbortSignal (A8), signout order (A9), + low (PostId brand, apiClient 204-throw/`ApiErrorBody` rename, toast `role=alert`, `aria-sort`, clamp page, Topbar link). Verify production `next build && next start`: routes static + CSP header + `<script>` bị sanitize; test web 28 + admin 37 + ui 3 xanh; security-review sạch.
  Spec: `docs/superpowers/specs/2026-07-11-slice5c-polish-fe-design.md` · Plan: `docs/superpowers/plans/2026-07-11-slice5c-polish-fe.md`.
- ✅ **Slice 5d — DONE (Backend Robustness, đợt 4)**: core resolve toàn bộ Medium — body limit `shared/bodylimit` (`MAX_BODY_BYTES` 2 MiB → 413 `PAYLOAD_TOO_LARGE`) (M4); `RequireAuth(sm, allowlist)` re-check allowlist mỗi request + destroy session khi email bị gỡ (M2); stats routes dời sang `GET /stats/posts*` — hết shadowing slug, admin đổi URL cùng commit (M3); tag upsert batch `ON CONFLICT (slug) DO UPDATE` atomic + `translateErr` scope về post (M1); **optimistic locking end-to-end** — cột `posts.version`, PUT bắt buộc `version`, conflict → 409 `VERSION_CONFLICT`, admin banner "Bài đã bị sửa ở nơi khác" + nút "Tải bản mới nhất" (M5). **Transactional outbox** (`platform/outbox`): bảng + ghi event `post.created/updated/deleted` cùng transaction với write, `Dispatcher` poll 10s + `LogHandler` pluggable (Phase 2 cắm consumer thật). 2 migration Atlas (`add_post_version`, `add_outbox`). Test admin 39 + web 28 + core xanh; security-review sạch; verify E2E (dispatcher verified live qua synthetic event).
  Spec: `docs/superpowers/specs/2026-07-12-slice5d-backend-robustness-design.md` · Plan: `docs/superpowers/plans/2026-07-12-slice5d-backend-robustness.md`.
- ✅ **Slice 5e — DONE (Dọn backlog Low)**: `NewS3Storage` trả interface `Storage` + `PresignExpires` cấu hình qua env (L8); lỗi bind JSON trả message generic cho client, chi tiết chỉ vào log — hết leak internals Go (L9); dọn orphan tags trong cùng transaction ngay sau update/delete post, scope theo đúng tag vừa gỡ (không quét/khoá toàn bảng) (L10); `getBoolEnv`/`getInt64Env` trả `error`, `Load` fail-fast khi env rác — verify live `SESSION_COOKIE_SECURE=ture` panic ngay khi start (L11, giờ RESOLVED đầy đủ); DB test riêng `blog_test` tự tạo qua `postgres-init` cách li khỏi dev DB + test đếm theo delta/token + dispatcher-test scope theo event id (L12). Toàn bộ finding Critical/High/Medium/Low **đã được yêu cầu** trong senior review giờ DONE; còn mở L1–L6 (low, chưa yêu cầu). Test core toàn bộ xanh (`go test ./...` với `blog_test`).
  Spec: `docs/superpowers/specs/2026-07-12-slice5e-low-backlog-design.md` · Plan: `docs/superpowers/plans/2026-07-12-slice5e-low-backlog.md`.
- ✅ **Slice 6 — DONE (Trang chủ blog "Mạch" — UI/UX)**: dựng lại pixel-close trang chủ tạp chí "Mạch" (phương án `2a` từ `design/`) trong `apps/web`. Kiến trúc **Server Component shell + client island** (`app/page.tsx` RSC fetch + adapt `Post→ArticleVM`, giữ ISR `revalidate=60`, SEO nguyên vẹn) → `MagazineBoard`. **Chống re-render**: Zustand selector + `React.memo` + `useDeferredValue` + hàm lọc/format thuần. **Token Mạch** scope riêng web (`globals.css` `:root`/`.dark` + Tailwind `bg-surface/text-fg/…`, category color một nguồn ở `categories.ts`, tint qua `color-mix`), font **Bricolage Grotesque + Be Vietnam Pro + Space Mono** qua `next/font` + script chống nháy. **Tương tác chạy cục bộ** qua interface service (bookmark localStorage, auth mock, newsletter) — có seam cắm backend thật sau; masthead/footer thành chrome toàn site. **Data gap** (author/views/comments backend chưa có) ẩn mềm; category = tag đầu, readTime tính từ content. Lazy `auth-modal`, skeleton `loading.tsx`, `next/image` (host `picsum.photos`). **Seed** `services/core/seed/seed_articles.sql` (~12 bài phủ 10 category). 56 test web xanh (34 mới cho Slice 6: lib/store/hook/component + 22 cũ), build production OK, **verify E2E live** (search lọc, dark mode, bookmark gate→modal→login mock→"Đã lưu N", SSR/SEO). **Backend consumer thật (auth/bookmark/newsletter) + redesign trang detail/tags còn ở phía trước.**
  Spec: `docs/superpowers/specs/2026-07-14-slice6-magazine-homepage-design.md` · Plan: `docs/superpowers/plans/2026-07-14-slice6-magazine-homepage.md`.
- ✅ **Slice 7 — DONE (Polish trang chủ Mạch)**: hệ token màu mới **chrome/ink/field** (light+dark đầy đủ trong `globals.css`, map Tailwind) — masthead+subnav bỏ băng xanh accent, chuyển nền kem `chrome-bg` hoà body (accent co về logo/CTA), footer chuyển **ink** mực nâu ấm; masthead+subnav bọc inner `mx-auto max-w-shell` (nâng **1200px**) thẳng hàng body/footer. Font display **Bricolage Grotesque → Playfair Display** (subset vietnamese). **Newsletter redesign**: hook `useNewsletterForm` (state machine idle/submitting/success/error, TDD 5 test) + form liền khối, lỗi inline `role=alert`/`aria-invalid`, success inline "✓ Đã đăng ký", microcopy; benefit line mới. **Perf**: `useCallback` cho `onOpen` (memo `ArticleRow`/`TopViewedList` hết bị inline-arrow phá), `contain-intrinsic-size` cho rows. **Animation framer-motion** dạng `LazyMotion strict + m.` với **features nạp async** (First Load `/` 101→119 kB, domAnimation tách khỏi bundle đầu): rows fade-up `whileInView` once, modal fade+scale, toast slide-in, `MotionConfig reducedMotion="user"`. 63 test web xanh; verify E2E live production (light/dark, search, bookmark gate→modal, newsletter error/success, SSR HTML giữ title/link bài).
  Spec: `docs/superpowers/specs/2026-07-15-slice7-mach-polish-design.md` · Plan: `docs/superpowers/plans/2026-07-15-slice7-mach-polish.md`.
- ✅ **Slice 8 — DONE (i18n en/vi cho web)**: **next-intl** (v3) — routing `as-needed` (**vi mặc định không prefix, mọi URL cũ giữ nguyên**; `/en/...` cho tiếng Anh), `localeDetection: false` (`/` luôn vi — canonical SEO, đổi ngôn ngữ chỉ qua switcher VI/EN trong masthead). App tree chuyển vào `app/[locale]/` (`setRequestLocale` + `generateStaticParams` giữ SSG/ISR; `sitemap/rss/robots` ở gốc). **Codegen `pnpm i18n:gen`** (`scripts/i18n-gen.ts` + hàm thuần `syncMessages` TDD): `messages/vi.json` là source of truth → sinh `messages.d.ts` (augment `IntlMessages` — sai key = compile error) + sync khung `en.json` (thiếu → `__TODO__`, thừa → xoá, đã dịch → giữ); **guard test** fail khi en lệch khung hoặc còn `__TODO__`. Nội dung bài viết giữ tiếng Việt ở cả 2 locale; chrome dịch toàn bộ (masthead/subnav/rail/sidebar/footer/newsletter/auth/toast/errors/detail/tags). Store toast chuyển **key + params** (component dịch); hook newsletter nhận `errors` inject; adapter `postsToArticleVMs(posts, labels)` nhận category label + readTime theo locale; mọi điều hướng nội bộ qua `Link/useRouter` của `@/i18n/navigation`. SEO: hreflang vi/en/x-default (layout + blog + tags), sitemap alternates 2 locale, OG locale, `<html lang>` động. 70 test web xanh; verify E2E live (`/` vi, `/en` chrome en + bài vẫn tiếng Việt, switcher giữ pathname, hreflang trong HTML).
  Spec: `docs/superpowers/specs/2026-07-15-slice8-i18n-design.md` · Plan: `docs/superpowers/plans/2026-07-15-slice8-i18n.md`.
- ✅ **Polish theo feedback (2026-07-16, cùng branch slice-8)**: **fix CSP dev** — `script-src` thiếu `'unsafe-eval'` làm react-refresh của `next dev` chết toàn bộ JS client (bài viết kẹt `opacity:0` của framer-motion, mọi nút không click được); giờ `'unsafe-eval'` CHỈ thêm ở dev, production giữ nguyên. Font display **Playfair Display → Roboto** (700/900, subset vietnamese). **Newsletter chuyển thành band full-width nổi bật** giữa nội dung và footer (input lớn, 2 cột desktop / dọc mobile), bỏ box nhỏ trong rail, giữ variant footer. **Responsive mobile**: rail `hidden lg:block` + **MobileCategoryBar** chips cuộn ngang (`lg:hidden`), sidebar `hidden xl:block`, masthead wrap (search full-width mobile, tagline ẩn), subnav cuộn + meta ẩn, article row bỏ thumbnail/side-meta trên mobile, footer grid 1→2→4 cột. Thêm `<noscript>` override để JS bị chặn vẫn đọc được bài.

- ✅ **Slice 9 — DONE (BE production-readiness)**: **logging** production (reqlog + `route/ip/ua/bytes_out/errors`, 5xx=ERROR, panic→slog, `LOG_LEVEL`, JSON prod → Loki); **Prometheus metrics** server riêng `:9091` (http theo route template, DB pool, cache, outbox, views, blurhash; pprof gated); **Redis cache-aside** cho read công khai với **key-versioning invalidation** (bump `posts:ver` O(1)), Redis chết → fallback DB; env mở rộng + fail-fast + `Config.LogValue` **redact secret**; **2 goroutine feature để học**: blurhash worker pool (buffered channel + N worker + enqueue non-blocking + graceful drain + `cmd/blurhash-backfill` errgroup SetLimit; SSRF guard tầng dial `Dialer.Control` + chặn decompression bomb qua DecodeConfig) và view counter batch (`POST /posts/:id/view` → channel → gom map → flush ticker/ngưỡng/shutdown; cột `views` + sort whitelist); **Docker multi-stage**: core→distroless (~95MB), web→Next standalone (`BUILD_WITHOUT_API=1` + helper `buildSafe` cho build không cần API), admin→nginx SPA; `docker-compose.prod.yml` + `docker-compose.observability.yml` (Prometheus/Grafana/Loki/Promtail, dashboard provisioned). **Verify E2E trên stack prod thật**: 9 container, Grafana healthy, Prometheus targets up, Loki nhận log JSON, 20 view flush batch, redis keys + tắt redis API vẫn 200, backfill blurhash 12/12, API trả `cover_blurhash`+`views`.
  Spec: `docs/superpowers/specs/2026-07-16-slice9-be-prod-readiness-design.md` · Plan: `docs/superpowers/plans/2026-07-16-slice9-be-prod-readiness.md`.
- ✅ **Slice 10 — DONE (FE image optimization + blurhash)**: `PostSchema` thêm `cover_blurhash`/`views`; **`CoverImage`** (khung `aspect-[16/9]` reserve chỗ → **CLS = 0**, `next/image fill` fade-in) + **`BlurhashCanvas`** (decode npm `blurhash` lên canvas 32px, placeholder hiện tức thì) áp cho blog detail + thumbnail `ArticleRow`; image optimize (`formats avif/webp`, `sizes` đúng khung, `priority` LCP, `quality 75`); **"Top xem nhiều" dùng views thật** (`listTopViewed` sort=views, fallback bài mới khi trống); **`ViewTracker`** beacon `POST /posts/:id/view` (sendBeacon → fetch keepalive, CSP thêm `connect-src` core API qua `NEXT_PUBLIC_API_URL`); `buildSafe` + `NEXT_OUTPUT_STANDALONE` (standalone chỉ bật trong Docker — Windows local EPERM symlink). 77 test web + 39 admin xanh; verify E2E live: blurhash canvas trong SSR, khung không shift, beacon tăng views trong DB (qua batch flush), image Docker web build lại OK.
  Spec: `docs/superpowers/specs/2026-07-16-slice10-fe-images-design.md` · Plan: `docs/superpowers/plans/2026-07-16-slice10-fe-images.md`.
- ✅ **Slice 11 — DONE (Redesign detail/tags theo chrome Mạch)**: `/blog/[slug]` **editorial Mạch** — kicker màu category (`categoryFromTags`), title `font-display` Roboto 900, meta mono `ngày · phút đọc · lượt xem` (views thật, ẩn khi 0), `.article-body` toàn bộ sang token Mạch (link accent, blockquote nền soft viền accent, code/pre soft/line, heading display), `TagBadge` chips tint màu category (helper mới `categoryColorForTag` — tag lạ → accent); **`RelatedPosts`** RSC 3 bài cùng tag đầu (`pickRelated` TDD, ẩn khi rỗng, thumbnail blurhash). `/tags` chips màu category; `/tags/[slug]`+`/page/[n]`: header `#tag` màu category + `{count} bài viết` mono, **`PostCard` row Mạch** (thumbnail 132×90 blurhash chống CLS, kicker màu, title display, excerpt clamp); pagination/error/not-found sang token, **xoá class theme cũ** `.article-kicker/.article-title`. i18n keys mới qua `i18n:gen`. 83 test web xanh, build 56 trang; verify E2E live (detail dark, related trên bài IT, `#it` + rows, `/en` bản dịch, chips color-mix đúng màu).
  Spec: `docs/superpowers/specs/2026-07-17-slice11-mach-detail-tags-design.md` · Plan: `docs/superpowers/plans/2026-07-17-slice11-mach-detail-tags.md`.
- ✅ **Slice 12 — DONE (Chrome toàn site + content images CLS 0)**: **Masthead + SubNav chuyển vào `app/[locale]/layout.tsx`** — mọi trang (detail/tags/404) đủ đầu-đuôi Mạch; SearchBar gõ từ trang khác tự điều hướng về `/` giữ query (store Zustand sống qua client-side nav). **Content images**: cột `posts.content_image_meta JSONB` (`{src: {w,h,ph}}`) — worker blurhash mở rộng nhận `ContentJob` (parse `content_html` bằng `ExtractImgSrcs` x/net/html dedupe cap 20, fetch qua SSRF guard, `EncodeMeta` = dims + blurhash render sẵn **PNG 32px data URI**), enqueue khi content đổi, bump cache version sau khi ghi; backfill pass 2 (fix bug return sớm + errgroup ctx bị cancel giữa 2 pass); API + `PostSchema` (`z.record(z.string(), ...)` — Zod v4) trả meta; web **`rehypeEnrichImages`** chạy SAU sanitize trong `sanitizeHtml(html, meta)`: img nhận `width/height` (browser reserve đúng tỉ lệ → **CLS 0**) + `style background:url(data:...)` placeholder + `loading=lazy decoding=async` (style user vẫn bị sanitize strip — có test chứng minh). 87 test web + 39 admin + core xanh; verify E2E live: HTML SSR có đủ width/height/style/lazy, masthead trên detail+tags, search-from-detail về home giữ query.
  Spec: `docs/superpowers/specs/2026-07-17-slice12-sitewide-chrome-content-images-design.md` · Plan: `docs/superpowers/plans/2026-07-17-slice12-chrome-content-images.md`.
- ✅ **Slice 13 — DONE (Backend consumer Mạch — auth người đọc + bookmark + newsletter + rate limit + view dedupe)**: thay toàn bộ mock localStorage bằng backend thật, thực thi **subagent-driven 15 task TDD**. **Module Go mới `internal/modules/readers`** (clean-lite): reader auth **Google OAuth BFF** tái dùng `auth.OAuthProvider`/`auth.Identity`/`NewGoogleProvider` nhưng flow riêng — session key `reader_id` (const exported `SessionKeyReaderID`) tách hẳn admin `admin_email`, giữ state+PKCE one-time + `RenewToken` anti-fixation + `EmailVerified` gate, **KHÔNG allowlist** (ai có Google verified đều đăng nhập); bookmark Postgres (`bookmarks` PK kép, idempotent `ON CONFLICT DO NOTHING`); newsletter `subscribers` **citext** độc lập (không cần login, always-201 không leak, email chuẩn hoá `mail.ParseAddress(...).Address` chống parser-differential). Route: `/auth/reader/{login,callback,logout,me}` (logout chỉ gỡ `reader_id`, không destroy admin), `/readers/me/bookmarks` (GET/PUT/DELETE sau `RequireReader`), `POST /subscribers`. **`shared/redirect.SafePath`** guard open-redirect cho `returnTo` (reject `//`,`/\`, scheme, control-char; chỉ nối sau `webBaseURL` cố định). **`shared/ratelimit.PerIP`** — Redis fixed-window per-IP (auth 10/min, subscribe 5/min, view 60/min), **fail-open** khi Redis chết, `nil`=no-op. **`posts.ViewDeduper`** — Redis SET+TTL 48h, 1 view/identity(`r:readerID` else `a:sha256(ip+salt)`)/bài/ngày, fail-open, wired vào `POST /posts/:slug/view` (deduped→202 không đếm); `posts` KHÔNG import `readers` (closure inject). Config: `ReaderRedirectURL`/`WebBaseURL`/`ViewDedupSalt` (fail-fast salt khi Redis on, LogValue redact) + `TRUSTED_PROXIES` (`SetTrustedProxies` → ClientIP không spoof XFF) + `cache.Redis.Client()` accessor. **FE `apps/web`**: `apiBookmarkService`/`apiNewsletterService` (`credentials:include`, throw non-ok), store `hydrate()` (`/auth/reader/me` + bookmarks, **`ReaderHydrator`** ở layout chạy mọi full-load), `toggleSave` optimistic + snapshot-rollback, `logout` API; **auth modal** = nút "Tiếp tục với Google" (`<a href>` full-page BFF redirect + `returnTo` giữ locale); bỏ `login()`/`MockUser`/`authMode` mock; i18n `googleCta`/`intro`/`saveError` vi+en. Migration Atlas `add_readers` (citext extension + FK cascade). **Backend `go test ./...` xanh + 101 test web + build production OK; final whole-branch review (opus, 0 Critical) → fix 3 Important + Minor.** **Verify E2E live (Google thật + Redis) là thao tác tay còn lại** — xem "📍 Điểm hiện tại". Nợ backlog: FK cascade chưa test, unsubscribe + reader profile/GDPR (Phase 2).
  Spec: `docs/superpowers/specs/2026-07-17-slice13-backend-consumer-design.md` · Plan: `docs/superpowers/plans/2026-07-17-slice13-backend-consumer.md`.
- ✅ **Slice 14 — DONE (CI/CD — GitHub Actions + GHCR, CD dormant)**: 3 workflow trong `.github/workflows/`. **`ci.yml`** (trigger `pull_request` + `push`, concurrency cancel-in-progress): job `changes` (`dorny/paths-filter` → `core`/`web`/`admin`, đổi `packages/**`+lockfile bật cả web+admin) → job **`core`** (Go 1.25.1 + Postgres 16 service container `pgvector/pgvector:pg16`, tạo `blog_test` bằng `psql`, `gofmt -l`/`go vet`/`go build`/`go test` với `TEST_DATABASE_URL`), job **`web`** + **`admin`** (pnpm 11.10.0 + node 20 cache, `pnpm turbo run lint test build --filter=@ultimate/<app>...`; admin `lint`/`build` = `tsc --noEmit` thỏa quy ước đồng bộ admin). **`release.yml`** (trigger `push: main`, `packages: write`): matrix 3 image build+push `ghcr.io/vule96/ultimate-website/{core,web,admin}` tag `latest`+`sha-<short>` qua `docker/build-push-action@v6` + buildx + cache `type=gha` scope theo image; auth GHCR bằng `GITHUB_TOKEN`; build-arg FE (`NEXT_PUBLIC_MEDIA_HOST`/`VITE_API_URL`) từ repo Variables `vars.*`. **`deploy.yml`** (**DORMANT** — chỉ `workflow_dispatch`, khối auto-trigger comment sẵn): SSH `appleboy/ssh-action@v1` → `docker compose up -d postgres` → one-shot `arigaio/atlas` migrate → `docker compose -f docker-compose.prod.yml -f docker-compose.deploy.yml pull+up -d` → retry healthcheck `/healthz`; secrets `VPS_HOST`/`VPS_USER`/`VPS_SSH_KEY` điền khi có server. **`docker-compose.deploy.yml`** override `build: !reset null` → `image: ghcr.io/...:${TAG:-latest}` (chồng lên prod file). Verify: **actionlint sạch cả 3 workflow** + `docker compose --env-file .env.prod -f prod -f deploy config` merge đúng (build reset, 3 image trỏ GHCR). Nợ: bật branch protection `main` (tay); image FE environment-specific; thiếu `NEXT_PUBLIC_API_URL` web; chưa `govulncheck`/Trivy; CD chưa verify live (chờ VPS).
  Spec: `docs/superpowers/specs/2026-07-18-slice14-cicd-design.md`.
- ✅ **Slice 15 — DONE (Next 16 + React 19 + next-intl v4 — full FE modernization)**: web **Next 14→16.2.10 + React 18→19.2 + next-intl v3→v4**, admin + `packages/ui` → **React 19**; vá 5 CVE Next 14 → **gỡ `.trivyignore.yaml`** (Trivy xanh không ignore). 7 phase verify riêng: (P1) ui peer `^18.2||^19` + @types 19; (P2) web deps bump; (P3) **async request APIs** (codemod `await params` 7 file), next-intl v4 (`NextIntlClientProvider locale` — codebase đã dùng API v4-style), vitest inline next-intl (resolve `next/navigation`), test mock `blurhash` + cast Metadata OG/Twitter union; (P4) **ESLint 9 flat** (`eslint-config-next` 16 native flat, `next lint`→`eslint .`, lint web lại vào CI, 0 error/12 warning); (P6) admin React 19 (`import type JSX` — React 19 bỏ global namespace). **pnpm overrides ép single react/react-dom/@types 19 toàn workspace** (dedupe — radix qua ui gây "Invalid hook call/useMemo null") + `pnpm-workspace allowBuilds` (sharp/swc/esbuild/unrs-resolver). **Turbopack build mặc định OK** (55 trang SSG + ISR giữ, không cần webpack fallback). Rule mới `react-hooks/set-state-in-effect` bắt use-theme (disable có justification). Verify: full workspace **8/8 turbo task** (ui/web/admin × lint/test/build) + tsc 0 lỗi + web 101/admin 39/ui 3 test + Docker web(Next 16 standalone)/admin(nginx-unprivileged) build — xanh local. Nợ: 12 ESLint warning polish (`<a href="/">`→`Link` masthead/footer); E2E live cuối (stack thật core+redis) chưa chạy.
  Spec: `docs/superpowers/specs/2026-07-18-slice15-next16-react19-design.md`.

**Storage & editor (từ 3c):**
- Object storage S3-compatible, upload theo **presigned PUT** (client upload thẳng, không qua core). **Quy ước chốt: DEV = MinIO, PROD = Cloudflare R2** — chỉ đổi env `STORAGE_*`, không sửa code. `.env` mặc định trỏ MinIO; khối R2 để comment (flip khi cần). **R2 đã verify end-to-end** (credential/CORS/public URL + upload ảnh qua admin OK). Thiết lập R2 (Account API Token, endpoint, public URL, **CORS bắt buộc** cho origin admin): xem `services/core/README.md` mục "Object storage". Web cần `NEXT_PUBLIC_MEDIA_HOST` cho `next/image`.
- `r2.dev` chỉ dùng dev (rate-limited); production nên gắn **Custom Domain** cho bucket mới có CDN cache/WAF thật.
- Editor chọn qua `VITE_EDITOR=tiptap|lexical` (mặc định **tiptap**). Thêm editor mới = implement `PostEditorProps` + thêm nhánh trong `EditorSwitch`.

**Stack core đã chốt khi code (khác đề xuất ban đầu trong analysis doc):**
- Backend `services/core`: **Gin + GORM + Atlas** (analysis doc gợi ý chi + sqlc; đã chọn Gin/GORM/Atlas — xem spec Slice 1). Kiến trúc mỗi module: **Clean-lite / Hexagonal** (domain → service → repository → handler).
- Auth: **Google OAuth (`golang.org/x/oauth2`) + session server-side qua `alexedwards/scs` (Postgres store)**; allowlist email + cookie SameSite/Secure cấu hình qua env (xem `.env.example`).
- DB dev: Postgres 16 + pgvector qua `docker-compose.yml` ở gốc repo.

**Chạy toàn bộ stack (Docker → core → web → admin):** xem **`README.md`** ở gốc repo (thứ tự khởi động, cổng/URL, lỗi hay gặp).
**Chạy & test core riêng:** xem `services/core/README.md` (quickstart, Atlas migration, test).
Tóm tắt nhanh: `docker compose up -d` → `cd services/core && ./atlas.exe migrate apply --env gorm --url "postgres://blog:blog@localhost:5432/blog?sslmode=disable"` → `cp .env.example .env && go run ./cmd/api` (server `:8080`, `GET /healthz`) → `pnpm --filter @ultimate/web dev` (`:3000`) + `pnpm --filter @ultimate/admin dev` (`:5173`).

## Quy ước làm việc

- **Khảo sát code:** luôn ưu tiên **CodeGraph** (`codegraph_explore` / `codegraph explore`) trước grep/Read khi tìm hiểu hoặc định vị code — một call trả về source + call path + blast radius.
- **Tài liệu:** file `.md` là source of truth. Mỗi lần thay đổi yêu cầu → sửa `.md` trước, rồi HỎI trước khi cập nhật bản `.html`/Artifact.
- **Quy trình dev:** mỗi slice đi qua brainstorm → spec (`docs/superpowers/specs/`) → TDD → verify end-to-end → commit.
- **Cập nhật tiến độ:** sau mỗi slice/milestone → cập nhật mục **"Trạng thái hiện tại"** + dòng **"📍 Điểm hiện tại"** trong `CLAUDE.md` (để phiên làm việc mới nắm ngay context — dự án thường đổi session).
- **Nhận xét chuyên gia (bắt buộc mỗi slice):** kết mỗi slice bằng **góc nhìn 0.1% chuyên gia trong lĩnh vực** về thứ vừa triển khai — còn thừa/thiếu gì, cải thiện gì, rủi ro/nợ kỹ thuật cần biết trước khi ra production. Thẳng thắn, cụ thể, ưu tiên theo tác động; nợ mới phát hiện note vào tracker hoặc backlog.
- **Issue tracking:** mọi issue/finding note vào **`docs/reviews/2026-07-11-senior-code-review.md`** (issue mới → thêm vào mục tương ứng theo mảng + severity, đặt mã mới nối tiếp C/H/M/L, A, W). Khi resolve issue nào → đánh dấu **`✅ RESOLVED (YYYY-MM-DD, commit <hash>)`** ngay tại finding đó (không xoá nội dung finding).
- **⚠️ Đồng bộ admin sau MỖI tính năng (bắt buộc):** `apps/admin` là consumer thứ 2 của core API — dễ bị bỏ quên khi làm BE/web. Sau mỗi slice/tính năng chạm **API (route/field/status), schema, hoặc `packages/types`/`packages/ui`**: (1) chạy `pnpm --filter @ultimate/admin exec tsc --noEmit && pnpm --filter @ultimate/admin build` (Zod ở `packages/types` là bản chép tay của struct Go → đổi field chỉ vỡ runtime, PHẢI cập nhật client admin cùng commit); (2) nếu backend thêm bảng/khái niệm người dùng cuối quản trị (vd `subscribers`, `readers` ở Slice 13 — hiện CHƯA có UI admin) thì note vào backlog để bổ sung trang quản trị; (3) đưa admin (typecheck + build) vào bước **verify end-to-end** của slice, đừng chỉ verify web. **Lưu ý stack Docker:** image build sẵn KHÔNG tự phản ánh code mới — sau khi merge phải `docker compose -f docker-compose.prod.yml build <service>` rồi `up -d`, nếu không `:3000`/`:5173`/`:8080` vẫn phục vụ bản cũ (đã dính 2026-07-18: image 07-16 khiến tags/detail thiếu chrome Slice 11/12 dù code đã đúng).
