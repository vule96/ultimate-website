# Slice 8 — i18n en/vi (next-intl + codegen) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** UI chrome của `apps/web` có 2 ngôn ngữ vi/en qua next-intl (vi mặc định không prefix, `/en/...`), messages đối xứng bằng lệnh `pnpm i18n:gen` (sinh types + sync khung en.json), SEO hreflang/sitemap 2 locale.

**Architecture:** App tree chuyển vào `app/[locale]/` (SSG/ISR giữ qua `setRequestLocale` + `generateStaticParams`); route metadata (`sitemap.ts`, `rss.xml`, `robots.ts`) ở lại gốc. `messages/vi.json` là source of truth; `scripts/i18n-gen.ts` (logic thuần `syncMessages` có test) sinh `en.json` skeleton + `messages.d.ts` augment type. Store toast chuyển từ string → key; category label + readTime đi qua translator ở adapter.

**Tech Stack:** next-intl ^3.26, tsx (devDep chạy script), Next 14 App Router, Vitest.

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-15-slice8-i18n-design.md`.
- `localePrefix: "as-needed"` — **mọi URL vi hiện tại giữ nguyên**, chỉ `/en/...` thêm mới.
- Nội dung bài viết (title/excerpt/content từ DB) KHÔNG dịch.
- `en.json` + `messages.d.ts` là file generated — không sửa tay `messages.d.ts`; `en.json` chỉ sửa phần dịch nghĩa.
- Guard trong test suite: en.json cùng khung key với vi.json và không còn `__TODO__`.
- Test: `pnpm --filter @ultimate/web test`. Build: `pnpm --filter @ultimate/web build` (prerender đủ 2 locale).
- Commit kết bằng `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: Cài next-intl + routing/navigation/request/middleware + plugin

**Files:**
- Modify: `apps/web/package.json` (dep `next-intl`)
- Create: `apps/web/src/i18n/routing.ts`, `apps/web/src/i18n/navigation.ts`, `apps/web/src/i18n/request.ts`, `apps/web/src/middleware.ts`
- Modify: `apps/web/next.config.mjs` (wrap `createNextIntlPlugin`)

**Interfaces:**
- Produces: `routing` (`locales: ["vi","en"]`, `defaultLocale: "vi"`), type `Locale = (typeof routing.locales)[number]`; `Link/usePathname/useRouter/redirect` từ `@/i18n/navigation` — mọi task sau dùng thay `next/link`/`next/navigation` cho điều hướng nội bộ.

- [ ] **Step 1:** `pnpm --filter @ultimate/web add next-intl@^3.26`

- [ ] **Step 2: `src/i18n/routing.ts`**

```ts
import { defineRouting } from "next-intl/routing";

export const routing = defineRouting({
  locales: ["vi", "en"],
  defaultLocale: "vi",
  localePrefix: "as-needed",
});

export type Locale = (typeof routing.locales)[number];
```

- [ ] **Step 3: `src/i18n/navigation.ts`**

```ts
import { createNavigation } from "next-intl/navigation";
import { routing } from "./routing";

export const { Link, redirect, usePathname, useRouter, getPathname } = createNavigation(routing);
```

- [ ] **Step 4: `src/i18n/request.ts`**

```ts
import { getRequestConfig } from "next-intl/server";
import { hasLocale } from "next-intl";
import { routing } from "./routing";

export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale;
  const locale = hasLocale(routing.locales, requested) ? requested : routing.defaultLocale;
  return {
    locale,
    messages: (await import(`../../messages/${locale}.json`)).default,
  };
});
```

(Nếu `hasLocale` không có ở bản 3.x đã cài → thay bằng `routing.locales.includes(requested as never)`.)

- [ ] **Step 5: `src/middleware.ts`**

```ts
import createMiddleware from "next-intl/middleware";
import { routing } from "./i18n/routing";

export default createMiddleware(routing);

export const config = {
  // Bỏ qua api, _next, file tĩnh (có đuôi), và route metadata gốc.
  matcher: ["/((?!api|_next|.*\\..*).*)"],
};
```

- [ ] **Step 6: `next.config.mjs`** — thêm đầu file:

```js
import createNextIntlPlugin from "next-intl/plugin";
const withNextIntl = createNextIntlPlugin("./src/i18n/request.ts");
```

và cuối file: `export default withNextIntl(nextConfig);` (bỏ `export default nextConfig;`).

- [ ] **Step 7:** Commit `feat(web): cài next-intl — routing as-needed vi/en, middleware, plugin`.

---

### Task 2: messages/vi.json + codegen `pnpm i18n:gen` (TDD sync) + en.json dịch đủ

**Files:**
- Create: `apps/web/messages/vi.json`
- Create: `apps/web/src/i18n/sync.ts` + Test: `apps/web/src/i18n/sync.test.ts`
- Create: `apps/web/scripts/i18n-gen.ts`
- Create (generated): `apps/web/messages/en.json`, `apps/web/src/i18n/messages.d.ts`
- Test: `apps/web/src/i18n/messages-guard.test.ts`
- Modify: `apps/web/package.json` (script `i18n:gen`, devDep `tsx`)

**Interfaces:**
- Produces: `syncMessages(vi: Json, en: Json) => { result: Json; added: string[]; removed: string[] }` (đệ quy, giữ thứ tự key theo vi, key thiếu → `"__TODO__ <giá trị vi>"`, key thừa → xoá, key đã dịch giữ nguyên); namespace messages các task sau dùng: `masthead, subnav, search, rail, sidebar, list, newsletter, auth, toast, footer, categories, errors, meta`.

- [ ] **Step 1: `messages/vi.json`** (source of truth):

```json
{
  "masthead": {
    "tagline": "Tạp chí tri thức",
    "themeToggleAria": "Đổi giao diện sáng/tối",
    "themeDark": "Tối",
    "themeLight": "Sáng",
    "langSwitchAria": "Switch to English"
  },
  "subnav": {
    "home": "Trang chủ",
    "categories": "Danh mục",
    "explore": "Khám phá",
    "meta": "SỐ 128 · 12.07.2026 · CẬP NHẬT MỖI NGÀY"
  },
  "search": { "placeholder": "Tìm bài viết, chủ đề, tác giả…" },
  "rail": { "heading": "Lĩnh vực" },
  "sidebar": {
    "trending": "Thịnh hành",
    "topViewed": "Top xem nhiều",
    "fbCta": "Tham gia nhóm Facebook →"
  },
  "list": {
    "latest": "Mới nhất",
    "results": "{count} kết quả",
    "empty": "Không tìm thấy bài viết phù hợp.",
    "saveAria": "Lưu bài viết",
    "unsaveAria": "Bỏ lưu",
    "comments": "{count} bình luận",
    "readTime": "{minutes} phút"
  },
  "newsletter": {
    "title": "Bản tin Mạch",
    "benefit": "Mỗi sáng thứ Hai · 5 phút đọc · Huỷ bất kỳ lúc nào",
    "placeholder": "Email của bạn",
    "submit": "Đăng ký",
    "success": "Đã đăng ký — hẹn bạn thứ Hai!",
    "privacy": "Không spam. Huỷ bằng 1 click.",
    "errInvalid": "Email không hợp lệ.",
    "errSystem": "Có lỗi xảy ra, thử lại sau nhé."
  },
  "auth": {
    "login": "Đăng nhập",
    "register": "Đăng ký",
    "logout": "Thoát",
    "savedCount": "Đã lưu {count}",
    "subtitleLogin": "Đăng nhập để lưu bài viết yêu thích.",
    "subtitleRegister": "Tạo tài khoản để lưu bài và nhận bản tin.",
    "namePlaceholder": "Tên hiển thị",
    "emailPlaceholder": "Email",
    "passwordPlaceholder": "Mật khẩu",
    "closeAria": "Đóng",
    "haveAccount": "Đã có tài khoản?",
    "noAccount": "Chưa có tài khoản?",
    "errInvalidEmail": "Email không hợp lệ.",
    "errNoPassword": "Vui lòng nhập mật khẩu."
  },
  "toast": {
    "authRequired": "Đăng nhập để lưu bài viết yêu thích.",
    "saved": "Đã lưu bài viết.",
    "unsaved": "Đã bỏ lưu."
  },
  "footer": {
    "desc": "Tạp chí tri thức cho người trẻ dám nghĩ — công nghệ, tài chính, kiến trúc, văn hoá và những gì đáng đọc mỗi ngày.",
    "colCategories": "Chuyên mục",
    "colAbout": "Về Mạch",
    "about": "Giới thiệu",
    "write": "Viết cho Mạch",
    "contact": "Liên hệ",
    "jobs": "Tuyển dụng",
    "colNewsletter": "Bản tin",
    "newsletterDesc": "Bài hay nhất mỗi sáng thứ Hai.",
    "copyright": "© 2026 Mạch. Bảo lưu mọi quyền.",
    "legal": "Điều khoản · Bảo mật · Cookie"
  },
  "categories": {
    "all": "Tất cả",
    "it": "IT",
    "ai": "AI",
    "finance": "Tài chính",
    "stock": "Chứng khoán",
    "arch": "Kiến trúc",
    "culture": "Văn hóa",
    "ent": "Giải trí",
    "news": "Tin tức",
    "growth": "Phát triển bản thân",
    "book": "Review sách"
  },
  "errors": {
    "notFoundTitle": "Không tìm thấy trang",
    "notFoundDesc": "Trang bạn tìm không tồn tại hoặc đã bị gỡ.",
    "errorTitle": "Đã xảy ra lỗi",
    "errorDesc": "Không tải được nội dung. Vui lòng thử lại.",
    "retry": "Thử lại",
    "backHome": "Về trang chủ"
  },
  "meta": { "description": "Mạch — tạp chí tri thức cho người trẻ." }
}
```

(Đối chiếu label trong `categories.ts` khi implement — dùng đúng label hiện có.)

- [ ] **Step 2: test `sync.test.ts` (fail trước)**

```ts
import { describe, expect, it } from "vitest";
import { syncMessages } from "./sync";

describe("syncMessages", () => {
  it("key thiếu → thêm __TODO__ kèm bản vi", () => {
    const { result, added } = syncMessages({ a: { b: "xin chào" } }, {});
    expect(result).toEqual({ a: { b: "__TODO__ xin chào" } });
    expect(added).toEqual(["a.b"]);
  });
  it("key thừa → xoá", () => {
    const { result, removed } = syncMessages({ a: "x" }, { a: "x-en", zombie: "old" });
    expect(result).toEqual({ a: "x-en" });
    expect(removed).toEqual(["zombie"]);
  });
  it("key đã dịch → giữ nguyên", () => {
    const { result } = syncMessages({ a: "vi" }, { a: "en" });
    expect(result).toEqual({ a: "en" });
  });
  it("thứ tự key theo vi.json", () => {
    const { result } = syncMessages({ b: "2", a: "1" }, { a: "one" });
    expect(Object.keys(result)).toEqual(["b", "a"]);
  });
  it("node lồng: en là string nhưng vi là object → thay bằng khung mới", () => {
    const { result } = syncMessages({ a: { b: "x" } }, { a: "flat" });
    expect(result).toEqual({ a: { b: "__TODO__ x" } });
  });
});
```

- [ ] **Step 3: implement `src/i18n/sync.ts`**

```ts
export type Messages = { [key: string]: string | Messages };

export function syncMessages(
  vi: Messages,
  en: Messages,
  prefix = "",
): { result: Messages; added: string[]; removed: string[] } {
  const result: Messages = {};
  const added: string[] = [];
  const removed: string[] = [];

  for (const [key, viVal] of Object.entries(vi)) {
    const path = prefix ? `${prefix}.${key}` : key;
    const enVal = en[key];
    if (typeof viVal === "string") {
      if (typeof enVal === "string") result[key] = enVal;
      else {
        result[key] = `__TODO__ ${viVal}`;
        added.push(path);
      }
    } else {
      const child = syncMessages(viVal, typeof enVal === "object" && enVal ? enVal : {}, path);
      result[key] = child.result;
      added.push(...child.added);
      removed.push(...child.removed);
    }
  }
  for (const key of Object.keys(en)) {
    if (!(key in vi)) removed.push(prefix ? `${prefix}.${key}` : key);
  }
  return { result, added, removed };
}
```

- [ ] **Step 4:** test pass → commit `feat(web): syncMessages — đồng bộ khung en theo vi (TDD)`.

- [ ] **Step 5: `scripts/i18n-gen.ts`**

```ts
/** Sinh messages/en.json (sync khung theo vi.json) + src/i18n/messages.d.ts (type-safe keys).
 *  Chạy: pnpm --filter @ultimate/web i18n:gen */
import { readFileSync, writeFileSync } from "node:fs";
import { fileURLToPath } from "node:url";
import path from "node:path";
import { syncMessages, type Messages } from "../src/i18n/sync";

const root = path.dirname(path.dirname(fileURLToPath(import.meta.url)));
const viPath = path.join(root, "messages/vi.json");
const enPath = path.join(root, "messages/en.json");
const dtsPath = path.join(root, "src/i18n/messages.d.ts");

const vi = JSON.parse(readFileSync(viPath, "utf8")) as Messages;
let en: Messages = {};
try {
  en = JSON.parse(readFileSync(enPath, "utf8")) as Messages;
} catch {
  /* en.json chưa tồn tại — sinh mới toàn bộ */
}

const { result, added, removed } = syncMessages(vi, en);
writeFileSync(enPath, JSON.stringify(result, null, 2) + "\n");

const dts = `// GENERATED by scripts/i18n-gen.ts — KHÔNG sửa tay. Chạy: pnpm i18n:gen
type ViMessages = typeof import("../../messages/vi.json");
declare interface IntlMessages extends ViMessages {}
`;
writeFileSync(dtsPath, dts);

const todo = JSON.stringify(result).match(/__TODO__/g)?.length ?? 0;
console.log(`i18n:gen — thêm ${added.length}, xoá ${removed.length}, còn __TODO__: ${todo}`);
```

- [ ] **Step 6:** `pnpm --filter @ultimate/web add -D tsx` + script trong `apps/web/package.json`: `"i18n:gen": "tsx scripts/i18n-gen.ts"`. Chạy `pnpm --filter @ultimate/web i18n:gen` → sinh `en.json` toàn `__TODO__` + `messages.d.ts`.

- [ ] **Step 7: dịch `messages/en.json`** (điền tay — giá trị chuẩn):

```json
{
  "masthead": {
    "tagline": "The knowledge magazine",
    "themeToggleAria": "Toggle light/dark theme",
    "themeDark": "Dark",
    "themeLight": "Light",
    "langSwitchAria": "Chuyển sang tiếng Việt"
  },
  "subnav": {
    "home": "Home",
    "categories": "Categories",
    "explore": "Explore",
    "meta": "ISSUE 128 · 12.07.2026 · UPDATED DAILY"
  },
  "search": { "placeholder": "Search articles, topics, authors…" },
  "rail": { "heading": "Fields" },
  "sidebar": {
    "trending": "Trending",
    "topViewed": "Most viewed",
    "fbCta": "Join our Facebook group →"
  },
  "list": {
    "latest": "Latest",
    "results": "{count} results",
    "empty": "No matching articles found.",
    "saveAria": "Save article",
    "unsaveAria": "Unsave",
    "comments": "{count} comments",
    "readTime": "{minutes} min"
  },
  "newsletter": {
    "title": "Mạch Newsletter",
    "benefit": "Every Monday morning · 5-min read · Unsubscribe anytime",
    "placeholder": "Your email",
    "submit": "Subscribe",
    "success": "Subscribed — see you Monday!",
    "privacy": "No spam. Unsubscribe in 1 click.",
    "errInvalid": "Invalid email address.",
    "errSystem": "Something went wrong, please try again."
  },
  "auth": {
    "login": "Log in",
    "register": "Sign up",
    "logout": "Log out",
    "savedCount": "Saved {count}",
    "subtitleLogin": "Log in to save your favorite articles.",
    "subtitleRegister": "Create an account to save articles and get the newsletter.",
    "namePlaceholder": "Display name",
    "emailPlaceholder": "Email",
    "passwordPlaceholder": "Password",
    "closeAria": "Close",
    "haveAccount": "Already have an account?",
    "noAccount": "No account yet?",
    "errInvalidEmail": "Invalid email address.",
    "errNoPassword": "Please enter a password."
  },
  "toast": {
    "authRequired": "Log in to save your favorite articles.",
    "saved": "Article saved.",
    "unsaved": "Removed from saved."
  },
  "footer": {
    "desc": "A knowledge magazine for young people who dare to think — technology, finance, architecture, culture and what's worth reading every day.",
    "colCategories": "Categories",
    "colAbout": "About Mạch",
    "about": "About us",
    "write": "Write for Mạch",
    "contact": "Contact",
    "jobs": "Careers",
    "colNewsletter": "Newsletter",
    "newsletterDesc": "The best reads, every Monday morning.",
    "copyright": "© 2026 Mạch. All rights reserved.",
    "legal": "Terms · Privacy · Cookies"
  },
  "categories": {
    "all": "All",
    "it": "IT",
    "ai": "AI",
    "finance": "Finance",
    "stock": "Stocks",
    "arch": "Architecture",
    "culture": "Culture",
    "ent": "Entertainment",
    "news": "News",
    "growth": "Personal growth",
    "book": "Book reviews"
  },
  "errors": {
    "notFoundTitle": "Page not found",
    "notFoundDesc": "The page you're looking for doesn't exist or was removed.",
    "errorTitle": "Something went wrong",
    "errorDesc": "Failed to load content. Please try again.",
    "retry": "Retry",
    "backHome": "Back to home"
  },
  "meta": { "description": "Mạch — a knowledge magazine for the young." }
}
```

- [ ] **Step 8: guard `src/i18n/messages-guard.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import vi from "../../messages/vi.json";
import en from "../../messages/en.json";
import { syncMessages, type Messages } from "./sync";

describe("messages en/vi đối xứng", () => {
  it("en.json đúng khung vi.json (chạy pnpm i18n:gen nếu fail)", () => {
    const { added, removed } = syncMessages(vi as Messages, en as Messages);
    expect(added).toEqual([]);
    expect(removed).toEqual([]);
  });
  it("en.json không còn __TODO__", () => {
    expect(JSON.stringify(en)).not.toContain("__TODO__");
  });
});
```

- [ ] **Step 9:** test pass toàn bộ → commit `feat(web): messages vi/en + lệnh i18n:gen (typegen + sync khung, guard đối xứng)`.

---

### Task 3: Chuyển app tree sang `app/[locale]/` + provider + test helper

**Files:**
- Move (git mv): `app/layout.tsx, page.tsx, loading.tsx, error.tsx, not-found.tsx, blog/, tags/, page/` → `app/[locale]/...` (giữ nguyên `globals.css, sitemap.ts, robots.ts, rss.xml/` ở `app/`)
- Modify: `app/[locale]/layout.tsx` (locale param, provider, `setRequestLocale`, `generateStaticParams`)
- Modify: các page dưới `[locale]` (nhận `params.locale`, gọi `setRequestLocale`)
- Create: `apps/web/src/test/render-intl.tsx`
- Modify: test component hiện có sang helper mới

**Interfaces:**
- Consumes: `routing`, messages Task 1–2.
- Produces: `renderWithIntl(ui: ReactElement)` — wrapper `NextIntlClientProvider locale="vi" messages={vi}` cho mọi test component.

- [ ] **Step 1:** `git mv` các file/thư mục page vào `apps/web/src/app/[locale]/` (import `./globals.css` trong layout đổi thành `../globals.css`).

- [ ] **Step 2: `app/[locale]/layout.tsx`** — thay đổi chính:

```tsx
import { NextIntlClientProvider, hasLocale } from "next-intl";
import { getMessages, getTranslations, setRequestLocale } from "next-intl/server";
import { notFound } from "next/navigation";
import { routing } from "@/i18n/routing";

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

export async function generateMetadata({ params }: { params: { locale: string } }) {
  const t = await getTranslations({ locale: params.locale, namespace: "meta" });
  return {
    metadataBase: new URL(SITE_URL),
    title: { default: SITE_NAME, template: `%s · ${SITE_NAME}` },
    description: t("description"),
  };
}

export default async function RootLayout({
  children,
  params: { locale },
}: {
  children: ReactNode;
  params: { locale: string };
}) {
  if (!hasLocale(routing.locales, locale)) notFound();
  setRequestLocale(locale);
  const messages = await getMessages();
  return (
    <html lang={locale} className={/* giữ font vars như cũ */} suppressHydrationWarning>
      <head>
        <script dangerouslySetInnerHTML={{ __html: THEME_SCRIPT }} />
      </head>
      <body className="flex min-h-screen flex-col bg-bg text-fg">
        <NextIntlClientProvider messages={messages}>
          <div className="flex-1">{children}</div>
          <MagazineFooter />
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

(`export const metadata` tĩnh cũ bỏ, thay bằng `generateMetadata` trên. Font declarations giữ nguyên.)

- [ ] **Step 3:** Mỗi page dưới `[locale]` thêm `params.locale` + `setRequestLocale(locale)` đầu hàm (home, blog/[slug], tags, tags/[slug], page/[n], tags/[slug]/page/[n]); `generateStaticParams` con giữ nguyên (Next tự nhân với locale từ layout).

- [ ] **Step 4: `src/test/render-intl.tsx`**

```tsx
import type { ReactElement } from "react";
import { render } from "@testing-library/react";
import { NextIntlClientProvider } from "next-intl";
import vi from "../../messages/vi.json";

export function renderWithIntl(ui: ReactElement) {
  return render(
    <NextIntlClientProvider locale="vi" messages={vi} timeZone="Asia/Ho_Chi_Minh">
      {ui}
    </NextIntlClientProvider>,
  );
}
```

- [ ] **Step 5:** Đổi các test component magazine (`article-row.test.tsx`, `magazine-board.test.tsx`, `newsletter-box.test.tsx`) sang `renderWithIntl`. `magazine-board.test.tsx` mock `@/i18n/navigation` thay `next/navigation` nếu đang mock router.

- [ ] **Step 6:** `pnpm --filter @ultimate/web test` + `build` xanh (build lúc này chrome vẫn hardcode vi — OK, Task 4 mới dịch) → commit `feat(web): app/[locale] + NextIntlClientProvider + renderWithIntl (SSG 2 locale)`.

---

### Task 4: Dịch chrome components + store toast key + adapter label/readTime + error pages + Link locale-aware

**Files:**
- Modify: `masthead.tsx, sub-nav.tsx, search-bar.tsx, auth-menu.tsx, auth-modal.tsx, newsletter-box.tsx, category-rail.tsx, category-rail-item.tsx (label qua prop), trending-chips.tsx, article-list.tsx, article-row.tsx, magazine-board.tsx, toast.tsx, magazine-store.ts, use-newsletter-form.ts (+ tests), article-vm.ts (+ test), categories.ts, app/[locale]/error.tsx, app/[locale]/not-found.tsx, features/posts/components/* (Link)` 
- Pattern chung: `const t = useTranslations("<ns>")` (client) / `getTranslations` (server).

**Interfaces:**
- Consumes: namespaces Task 2, `Link/useRouter` từ `@/i18n/navigation`.
- Produces: `ToastKey = "authRequired" | "saved" | "unsaved"` (store lưu key, `Toast` dịch); `useNewsletterForm(service, errors: { invalid: string; system: string })`; `postsToArticleVMs(posts, labels: { category(key: CategoryKey): string; readTime(minutes: number): string })`.

- [ ] **Step 1: store** — `MagazineState.toast: ToastKey | null`; `toggleSave` set `"saved" | "unsaved"`, gate set `"authRequired"`; export `type ToastKey`. `toast.tsx` → `const t = useTranslations("toast"); … {t(toast)}`. Sửa `magazine-store.test.ts` assert theo key.
- [ ] **Step 2: hook newsletter** — chữ ký mới `useNewsletterForm(service, errors)`; message lỗi lấy từ `errors.invalid`/`errors.system`. Sửa test hook truyền `{ invalid: "Email không hợp lệ.", system: "Có lỗi xảy ra, thử lại sau nhé." }`.
- [ ] **Step 3: adapter** — `postsToArticleVMs(posts, labels)`; `categoryLabel = labels.category(key)`, `readTime = labels.readTime(minutes)`. Home page (server): `const t = await getTranslations(); postsToArticleVMs(posts, { category: (k) => t(`categories.${k}`), readTime: (m) => t("list.readTime", { minutes: m }) })`. Sửa `article-vm.test.ts` truyền labels stub.
- [ ] **Step 4:** Thay chuỗi từng component theo bảng key Task 2 (client: `useTranslations`; giữ nguyên markup/class); `categories.ts` bỏ dùng `label` trực tiếp trong component — rail/chips/footer render `t(\`categories.${c.key}\`)` (giữ field `label` làm fallback data, không render).
- [ ] **Step 5:** Điều hướng: `article-list.tsx`, `magazine-board.tsx`, `sub-nav.tsx`, `error/not-found`, `features/posts/components/{pagination,post-card,posts-page,tag-badge}.tsx`, `app/[locale]/blog/[slug]/page.tsx`, `tags/*` — đổi `next/link`/`next/navigation` → `@/i18n/navigation` (Link/useRouter). `not-found.tsx`/`error.tsx` dùng `useTranslations("errors")` (not-found là RSC → `getTranslations`).
- [ ] **Step 6:** `pnpm --filter @ultimate/web test` xanh (sửa assertion text nếu đổi), `build` xanh → commit `feat(web): dịch toàn bộ chrome qua next-intl (store key, hook inject, adapter labels)`.

---

### Task 5: Language switcher VI/EN trong Masthead

**Files:**
- Create: `apps/web/src/features/magazine/components/lang-switcher.tsx`
- Modify: `masthead.tsx` (đặt cạnh nút theme)
- Test: thêm case trong `magazine-board.test.tsx` (switcher hiển thị)

- [ ] **Step 1:**

```tsx
"use client";
import { useLocale, useTranslations } from "next-intl";
import { usePathname, useRouter } from "@/i18n/navigation";

export function LangSwitcher() {
  const locale = useLocale();
  const t = useTranslations("masthead");
  const pathname = usePathname();
  const router = useRouter();
  const next = locale === "vi" ? "en" : "vi";
  return (
    <button
      onClick={() => router.replace(pathname, { locale: next })}
      aria-label={t("langSwitchAria")}
      className="rounded-lg border border-chrome-line bg-soft px-[13px] py-[9px] font-mono text-[11px] font-bold uppercase text-chrome-fg"
    >
      {next.toUpperCase()}
    </button>
  );
}
```

- [ ] **Step 2:** Masthead chèn `<LangSwitcher />` trước nút theme. Test + commit `feat(web): language switcher vi/en giữ pathname`.

---

### Task 6: SEO đa ngôn ngữ

**Files:**
- Modify: `app/[locale]/layout.tsx` + `app/[locale]/blog/[slug]/page.tsx` + tags pages (`alternates.languages`), `app/sitemap.ts` (alternates), `features/posts/metadata.ts` (OG locale)

- [ ] **Step 1:** `generateMetadata` layout thêm:

```ts
alternates: {
  languages: { vi: "/", en: "/en", "x-default": "/" },
},
openGraph: { locale: params.locale === "en" ? "en_US" : "vi_VN" },
```

Trang con (blog/[slug], tags/[slug], page/[n]): `alternates.languages` theo path tương ứng (`vi: /blog/x`, `en: /en/blog/x`, `x-default` = vi).

- [ ] **Step 2:** `sitemap.ts` mỗi entry thêm `alternates: { languages: { vi: url, en: url.replace(SITE_URL, `${SITE_URL}/en`) } }`. `rss.xml` giữ vi-only.

- [ ] **Step 3:** Verify build: HTML `/` chứa `hreflang="en"`, `/en` chứa `lang="en"` → commit `feat(web): hreflang + sitemap alternates + OG locale`.

---

### Task 7: Verify tổng + docs

- [ ] **Step 1:** `pnpm --filter @ultimate/web test` (toàn bộ, gồm guard) + `pnpm --filter @ultimate/web build` — route table có cả `/en/...` prerender.
- [ ] **Step 2:** E2E live production: `/` chrome vi (URL không prefix), `/en` chrome en + bài viết vẫn tiếng Việt; switcher đổi locale giữ pathname (`/en` ↔ `/`); newsletter/search/bookmark hoạt động 2 locale; hreflang trong HTML; `sitemap.xml`/`rss.xml`/`robots.txt` vẫn 200.
- [ ] **Step 3:** Cập nhật CLAUDE.md (Slice 8 DONE + 📍) → commit, merge branch.
